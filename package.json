{
  "name": "glomart-cloudtype-scrap-switch",
  "version": "3.1.0",
  "private": true,
  "description": "Glomart Cloudtype Playwright search switch server with start-time chromium install",
  "main": "index.js",
  "scripts": {
    "postinstall": "npx playwright install chromium",
    "start": "npx playwright install chromium && node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "playwright": "^1.44.1"
  },
  "engines": {
    "node": ">=18"
  }
}
